<%= turbo_frame_tag "main_content" do %>
  <div class="space-y-6">
    <div class="flex items-center justify-between">
      <h1 class="text-3xl font-bold text-gray-dark">Edit Appointment</h1>
      <%= link_to "Back to Appointments", appointments_path, data: { turbo_frame: "main_content" }, class: "text-light-blue hover:text-dark-blue" %>
    </div>

    <%= render 'form', appointment: @appointment %>
  </div>
<% end %>
